import styled from 'styled-components';
import signInBackgroundImg from '../../assets/mountain.png';

export const Background = styled.div`
    flex: 1;
    background: url(${signInBackgroundImg}) no-repeat center;
    background-size: cover;
`;